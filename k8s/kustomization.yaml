apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ncba-loop

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app: rdas
      managed-by: kustomize
    includeTemplates: true
